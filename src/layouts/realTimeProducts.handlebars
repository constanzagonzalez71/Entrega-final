<div class="container my-5">
  <h1 class="text-center mb-4">📡 Productos en Tiempo Real</h1>

  <!-- ✅ Alerta visual -->
  <div id="liveAlert" class="alert alert-success fade show text-center" role="alert" style="display: none;">
    🔄 ¡Lista de productos actualizada!
  </div>

  <!-- ✅ Formulario para nuevos productos -->
  <form id="formNewProduct" class="row g-3 mb-5">
    <div class="col-md-6">
      <input type="text" name="title" class="form-control" placeholder="Título" required>
    </div>
    <div class="col-md-6">
      <input type="number" name="price" class="form-control" placeholder="Precio" required>
    </div>
    <div class="col-12">
      <input type="text" name="description" class="form-control" placeholder="Descripción" required>
    </div>
    <div class="col-md-4">
      <input type="number" name="stock" class="form-control" placeholder="Stock" required>
    </div>
    <div class="col-md-4">
      <input type="text" name="code" class="form-control" placeholder="Código" required>
    </div>
    <div class="col-md-4">
      <input type="text" name="category" class="form-control" placeholder="Categoría" required>
    </div>
    <div class="col-12 text-end">
      <button type="submit" class="btn btn-success">➕ Enviar</button>
    </div>
  </form>

  <!--  Lista dinámica de productos -->
  <div id="productList" class="row justify-content-center">
    {{#each products}}
      <div class="card col-md-3 m-2 shadow-sm p-3 border-top border-info fadeIn">
        <h5>{{this.title}}</h5>
        <p><i class="bi bi-currency-dollar"></i> {{this.price}}</p>
        <p><i class="bi bi-box"></i> Stock: {{this.stock}}</p>
        <p><i class="bi bi-tags"></i> Categoría: {{this.category}}</p>
      </div>
    {{/each}}
  </div>
</div>

<!-- Scripts externos -->
<script src="/socket.io/socket.io.js"></script>
<script src="/js/index.js"></script>